"""Application configuration via pydantic-settings v2 (SPEC §10).

Skeleton stub. The concrete ``Settings`` model (``SettingsConfigDict`` with the
``ASTROPATH_`` env prefix and ``SecretStr`` bootstrap secrets) is implemented in
T-M0-05.
"""
