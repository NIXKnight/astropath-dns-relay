"""Standard-library logging configuration (SPEC §11.4).

Skeleton stub. ``configure_logging()`` (``logging.config.dictConfig`` with a
stdout console handler and a secret-redaction filter) is implemented in
T-M0-06; explicit deny rules are extended in T-M6-02.
"""
